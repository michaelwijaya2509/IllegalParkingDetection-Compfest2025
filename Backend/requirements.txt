# --- Web server & utils
flask
flask-cors
requests
python-slugify
yt-dlp

# --- CV / DL stack
ultralytics
opencv-python
deep-sort-realtime
numpy
Pillow

# --- PyTorch (CPU default). Ganti dengan wheel CUDA sesuai GPU-mu (lihat catatan di bawah).
torch
torchvision

# --- Firebase
firebase-admin

# --- Async / parsing / HTML
aiohttp
feedparser
beautifulsoup4

# --- Google Generative AI
google-genai

# --- MCP (Model Context Protocol) client & server
mcp
